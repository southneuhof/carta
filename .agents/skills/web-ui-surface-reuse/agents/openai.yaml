interface:
  display_name: "Web UI Surface Reuse"
  short_description: "Find and reuse the web framework surfaces"
  default_prompt: "Use $web-ui-surface-reuse to select framework components before implementing this web UI change."
