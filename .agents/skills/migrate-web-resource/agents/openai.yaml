interface:
  display_name: "Migrate Web Resource"
  short_description: "Migrate one web module to schema actions"
  default_prompt: "Use $migrate-web-resource to migrate one ADS-HK web module to the approved schema and per-action resource API."
