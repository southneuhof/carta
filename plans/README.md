# ADS-HK implementation plans

Generated by the Improve skill on 2026-08-10 at commit `abb232f`.
These are the remaining plans only. Plans 001–004 repair the shared web form
path. Plans 005–008 then implement the manual PTS slice against the legacy
system as the business reference. Plan 009 is an optional framework repair.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Repair shared active form fields | P1 | S | — | TODO |
| 002 | Complete the Role creation form contract | P1 | S | 001 | TODO |
| 003 | Make User creation resource-driven | P1 | M | 001 | TODO |
| 004 | Display page render errors | P2 | S | 001 | TODO |
| 005 | Establish the manual PTS parity ledger | P1 | S | 001–004 | TODO |
| 006 | Align manual PTS prerequisite master data | P1 | L | 005 | TODO |
| 007 | Make manual PTS master-data forms usable | P1 | M | 006 | TODO |
| 008 | Complete manual PTS field and screen parity | P1 | L | 005, 007 | TODO |
| 009 | Harden framework renderer and source merging | P2 | M | 001 | BLOCKED — requires explicit framework approval |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with a one-line reason) |
REJECTED (with a one-line reason).

## Dependency notes

- Complete Plan 001 first. It restores resource-form rendering for all later
  administration and PTS forms.
- Plans 002, 003, and 004 can run in parallel after Plan 001.
- Start manual PTS work only after Plans 001–004 are complete. Plan 005 is the
  field-parity contract for the remaining PTS implementation.
- Plan 006 changes database and API contracts before Plan 007 makes master
  data forms usable.
- Plan 008 uses the ledger and stable master-data lookups. It owns only the
  manual PTS report and actions, not Quality Inspection PTS.
- Plan 009 is optional and remains blocked until the user explicitly approves
  changes to `packages/is-vue-framework`. It is not required for Plans 005–008.

## Findings considered and rejected

- Copy the input catalog: rejected. All three files in
  `apps/web/src/routes/(demo)/input-catalog/` have the same SHA-256 values as
  `/Users/gamer/Documents/projects/is-framework/apps/web/src/routes/(demo)/input-catalog/`.
- Copy legacy integer IDs, Laravel audit columns, soft-delete flags, or the
  row `role_id`: rejected. Current UUIDs, current audit fields, and server-side
  permission checks replace them.
- Restore legacy role-ID workflow branches: rejected. The old transition uses
  magic role ID `316`; the new workflow uses explicit project permissions.
- Add a source adapter for `switch`: rejected. A switch has no option source;
  accepting one hides an invalid field configuration instead of repairing it.
