---
"@southneuhof/landing-sveltekit-framework": patch
---

Add `fieldsType` to `SectionSchemaMetaEditor` types so section schema meta display type mappings are supported in downstream TypeScript consumers.
